rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // SECURITY: Only allow authenticated users to access and modify the clinic data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
